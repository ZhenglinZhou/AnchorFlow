window.addEventListener('load', function () {
    make_carousel(
        'results-manipulation',
        manipulation_carousel_item_template,
        manipulation_items,
        3,
        1
    );
});
